<?xml version="1.0" encoding="utf-8"?>

<resources>

    <style name="Theme.Material.DayNight.NoActionBar" parent="@android:style/Theme.Material.Light.NoActionBar" />

    <!-- TODO : Change the themes' names -->
    <style name="Theme.Compose.Template" parent="Theme.Material.DayNight.NoActionBar">
        <!-- TODO : Change the status bar color -->
        <item name="android:statusBarColor">@color/blue_900</item>
    </style>

    <style name="Theme.Compose.Template.Splash" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
